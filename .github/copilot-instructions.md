# PokeDraft project notes

- Keep game rules in `src/game`; UI code belongs in `src/ui`.
- Preserve browser-only compatibility and relative asset paths for GitHub Pages.
- Prefer small rule-level tests in `tests` when changing the engine.
- Do not add a backend or runtime API dependency to the first version.
